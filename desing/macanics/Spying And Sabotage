requires:
    1) dialog update
    2) dialog interupts
    3) AI upgrade
    ?) pmc update
    
the capacity to preform 'spy' based actions could be interesting.

basically, one could send spy's off to create effects.
I should probably use nexerlins 'agent' system for this. it just makes more sense.
A lot to consider here.